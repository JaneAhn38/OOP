/*
#include <iostream>

#include <vector>
#include<algorithm> // For sorting

using namespace std;



int main() {
	vector < int> A;
	int i, j;

	cout << "Input 5 integer numbers: " << endl;
	for (i = 0; i <= 4; i++) {
		cin >> j;
		A.push_back(j); // pushback을 쓰면 값이 넣어짐.
	}

	for (i = 0; i <= 4; i++) {
		cout << A[i] << ", ";
	}
	cout << endl;

	sort(A.begin(), A.end());
	for (i = 0; i <= A.size() -1; i++) {
		cout << A[i] << ", ";
	}

	return 0;
}
*/